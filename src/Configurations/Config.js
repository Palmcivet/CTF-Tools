/**
 * @description: 配置文件，定义常量
 * @author: Palm Civet
 */

const ThemeType = {
	BLUE: "blue",
	GREEN: "green",
	YELLOW: "yellow",
	ORANGE: "orange",
};

const cipherMode = {
	DECODE: "decode",
	ENCODE: "encode",
	BRUTE: "brute",
};

const widget = {};

export { ThemeType };
